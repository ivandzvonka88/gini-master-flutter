import 'package:flutter/material.dart';

class AuthLoginTabBarDescription extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Sign in to your account",
      style: TextStyle(color: Color.fromRGBO(128, 139, 149, 1), fontSize: 16.0),
    );
  }
}
