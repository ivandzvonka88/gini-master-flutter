import 'package:flutter/material.dart';

class AuthRegisterTabBarDescription extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Create your account to access assistant",
      style: TextStyle(color: Color.fromRGBO(128, 139, 149, 1), fontSize: 16.0),
    );
  }
}
