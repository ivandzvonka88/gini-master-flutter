import 'package:flutter/material.dart';

class InChatProductsUnavailable extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("product is unavailable!"),
    );
  }
}
